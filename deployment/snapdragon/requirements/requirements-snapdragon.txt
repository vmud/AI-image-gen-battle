# Snapdragon X Elite Requirements - Research-Backed Versions (Aug 2025)
# Optimized for ARM64 Windows and NPU acceleration
# Based on compatibility testing and stability analysis

# Core Python packages (ARM64 compatible)
numpy>=1.21.0,<2.0.0          # Standard numpy (no MKL for ARM64)
pillow>=8.0.0,<11.0.0          # Image processing
requests>=2.25.0,<3.0.0        # HTTP library
psutil>=5.8.0                  # System utilities

# Web framework for demo
flask==2.3.3                   # Stable web framework
flask-socketio==5.3.6          # WebSocket support
Flask-CORS>=3.0.0              # CORS support for web API (note: capital letters)
eventlet>=0.33.0,<0.36.0       # Async networking for SocketIO

# ONNX Runtime - NPU Acceleration (CRITICAL: Use 1.18.1, NOT 1.19.0)
onnxruntime==1.18.1            # Stable base runtime
onnxruntime-directml           # DirectML NPU provider (primary)
# onnxruntime-qnn              # QNN native NPU (experimental, may not be available)

# PyTorch - ARM64 Compatible Versions
torch==2.1.2                   # ARM64 stable version (NOT 2.4+)
torchvision==0.16.2           # Compatible with torch 2.1.2

# Hugging Face - Conservative Versions (Tested for Windows ARM64)
transformers==4.36.2           # NOT 4.55+ (has Windows compatibility issues)
diffusers==0.25.1             # Stable diffusion pipelines
accelerate==0.25.0            # Training/inference acceleration
safetensors==0.4.1            # Safe model serialization
huggingface_hub==0.24.6       # Model hub access

# Additional optimizations
optimum                        # ONNX model optimization (latest compatible)

# Development dependencies (optional)
jupyter                       # For model testing
matplotlib                    # For visualization
tqdm                          # Progress bars

# Notes:
# - All versions tested on Snapdragon X Elite Windows 11 24H2
# - DirectML is primary NPU provider (more mature than QNN)
# - QNN support is experimental and may not install
# - PyTorch 2.1.2 is the latest stable ARM64 version
# - Transformers 4.36.2 avoids Windows compatibility issues in 4.55+
# - ONNX Runtime 1.18.1 is stable; 1.19.0 has GPU provider issues
